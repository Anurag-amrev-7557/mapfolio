name: Keep Backend Alive - 2
on:
  schedule:
    - cron: '4-56/12 * * * *'  # Every 12 minutes, starting at 4 minutes past the hour
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping services
        run: |
          curl -f https://mapfolio-e4ym.onrender.com/health
          curl -f https://mapfolio-ai-services.onrender.com/health
          echo "Pinged at $(date)"